import React from 'react';
import * as UI from './ui';

export default class Loading extends React.Component {
  render() {
    return (
      <UI.Screen.Column style={{backgroundColor: 'green'}}>
      </UI.Screen.Column>
    )
  }
}
